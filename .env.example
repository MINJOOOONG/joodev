# Database
DATABASE_URL="postgresql://user:password@localhost:5432/joodev_blog"

# Admin password hash (generate with: node -e "require('bcryptjs').hash('yourpassword', 12).then(console.log)")
ADMIN_PASSWORD_HASH="$2a$12$..."

# Session secret (generate with: openssl rand -base64 32)
SESSION_SECRET="your-session-secret-here"

# Vercel Blob
BLOB_READ_WRITE_TOKEN="vercel_blob_..."
